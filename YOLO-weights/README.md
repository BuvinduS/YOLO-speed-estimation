# YOLO Weights Directory

This directory is used to store YOLO model weights.

The required weights (e.g., `yolov8n.pt`) will be automatically downloaded
by Ultralytics the first time the project is run.

Model weight files are intentionally not committed to the repository.